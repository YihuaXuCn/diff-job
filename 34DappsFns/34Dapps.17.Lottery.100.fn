functiongetPrice()returns(uint){
require(owner==msg.sender);
addresswinner=winnerAddress();
if(winner==owner){
owner.transfer(address(this).balance);
}else{
uinttoTransfer=address(this).balance/2;
winner.transfer(toTransfer);
owner.transfer(address(this).balance);
}
returnaddress(this).balance;
}
